# aeps-llm-wiki-plugin 第三方依赖清单
#
# 实际依赖文件在: src/scripts/requirements.txt
# 原因:init 时 scripts/requirements.txt 会随 scripts/ 一起被拷贝到 <project>/scripts/requirements.txt
# 本文件保留为空占位,以防插件维护者 / 测试脚本误以为依赖列表在这里

# 安装方式:
#   pip install -r scripts/requirements.txt

# 添加新依赖请直接编辑 scripts/requirements.txt